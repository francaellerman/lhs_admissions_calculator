from setuptools import find_packages, setup

setup(name='naviance_admissions_calculator',
      version='0.0.1',
      install_requires = [
          'numpy',
          'pandas',
          'bs4',
          'statsmodels']
      )
